#ifndef QTGMS_RESOURCEMIMEDATA_H
#define QTGMS_RESOURCEMIMEDATA_H

// A UTF-8 project resource path used to assign an existing reference. Keep it
// separate from file URLs, which initiate resource/project imports.
static const char ResourceReferenceMime[] = "application/x-qtgms-resource-reference";

#endif
